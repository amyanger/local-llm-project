# PyTorch nightly with CUDA 12.8 (for RTX 5090 Blackwell support)
# Install separately with:
# pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128

# Core training libraries
transformers>=4.45.0
peft>=0.13.0
accelerate>=0.34.0
bitsandbytes>=0.44.0
datasets>=3.0.0
trl>=0.11.0

# Unsloth for faster training (2.5x speedup)
unsloth

# Utilities
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=5.0.0
huggingface_hub>=0.25.0

# Evaluation
evaluate>=0.4.0

# Jupyter for experimentation
jupyter
ipywidgets

# Monitoring
wandb
tensorboard

# Web UI
gradio>=4.0.0
